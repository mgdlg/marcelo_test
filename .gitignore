# Never commit ClickUp credentials or the row-level export they produce.
# The token is an env var (CLICKUP_TOKEN) and is revoked at offboarding.
.env
.env.*
clickup_raw_snapshot*.json
**/clickup_raw_snapshot*.json

# OS / editor noise
.DS_Store
Thumbs.db
